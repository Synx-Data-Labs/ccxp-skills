# Epics

(No epics defined yet — the maintainer hasn't written any `### E<n>` entries
below. This file simulates that pre-authoring state: it exists and is
readable, but `epic_parse_file` should yield zero EPIC records.)

See README.md's "dev/EPICS.md format" section for the shape a real entry
takes once one is added.
